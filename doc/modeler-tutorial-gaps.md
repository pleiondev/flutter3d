# Modeler tutorial — gaps journal

`tut-00`'s own "Журнал гэпов": every gap found while writing or walking
through a tutorial case, as one row. Accepted when this file is empty, or
every row below has become a `tut-NN` line in `doc/model-editor-plan.md`
with a decision — "делаем" or "откладываем" — recorded there.

| case | step | expected | actual | type | plan row |
|---|---|---|---|---|---|
| 1 | import (screen 21) | an agent driving the modeler over MCP can bring in an STL with a chosen unit and up axis, the way the app's own import screen (`import_plan.dart`, `screen/files.dart`) lets a person choose "mm" and weld/fix-normals/triangulate | **Resolved 2026-09-15 (tut-01, first half).** Used to take only a path: `ModelSession.import`/the `import` MCP tool had no `ImportOptions`, no weld/fix-normals/triangulate flags, even though `import_into.dart` and `fromModelDocument` both already took an `ImportOptions` — nothing above the session layer exposed it. Fixed by widening `ModelSession.import` to an optional `ImportOptions` plus `weld`/`fixNormals`/`triangulate`, all defaulting to the prior no-options behaviour, and extending the `import` MCP tool's schema with matching `unit`/`upAxis`/`weld`/`fixNormals`/`triangulate` fields — the identical choice a person gets from `import_plan.dart`'s own import screen, restricted to only the objects a given import itself just added. Doing this surfaced a real, separate bug in `importInto`: its own object-rebuild loop reset `ModelObject.version` to the constructor default on every re-numbered object, invisible while every import ran with the default `ImportOptions()` (nothing ever bumped a root's version) and real once one does not (`fromModelDocument`'s own scale/up-axis adjustment bumps a root's version via `copyWith` before `importInto` ever sees it) — fixed by carrying `object.version` across in the same loop. Case 1's own fixture now builds its starting project through `session.import(stlPath, options: ImportOptions(scale: 0.001), weld: true)` instead of calling `fromModelDocument`/`importMeshData` directly; regenerating produced byte-identical fixtures, confirming the two paths agree — the import step is still not on the case's own journal, for the unrelated reason given below (`ReplaceDocument` is not journalable). New coverage: `import_test.dart`'s own `unit: mm` vs `fromModelDocument` equivalence case and a no-options regression guard. The second half of this row — `readiness.dart`'s `MeshChecks` skipping `ImportedGeometry` — is unchanged; see the next row | function, agent-facing, resolved (first half) | tut-01 |
| 1 | diagnosis (mesh readiness) | `readiness.dart`'s `MeshChecks` (degenerate faces, non-manifold vertices, inverted shells) diagnose every geometry an object can hold | `_issuesWith`'s own `switch` only calls `MeshChecks` for `EditedGeometry`; an `ImportedGeometry` object — what any import is until something converts it — gets only `_morphTargetIssues`, so a freshly imported file with a genuinely broken mesh reads "ready to export" until the weld checkbox (or a mesh command) has turned it into an `EditedGeometry`. Real for case 1: `teapot.stl`, unwelded, shows zero issues; welded, it shows the one non-manifold vertex `MeshChecks` actually finds | integrity | tut-01 |
| — | `session.cleanup()`/`ModelHistory`'s journal marker | a recipe that finds nothing to do costs nothing on the undo stack (`ModelHistory.endTransaction`'s own doc comment: "a transaction in which nothing succeeded leaves no step") | true for `history`, not for `_journal`: `CommandJournal.beginTransaction`/`endTransaction` write their `{"transaction":"begin"}`/`{"transaction":"end"}` markers unconditionally, so a no-op `cleanup()` still writes two inert lines to the recovery journal (harmless on replay — an empty transaction is a no-op there too — but a person reading the raw `.jsonl` sees a pointless pair). Visible on `case1.jsonl` | UX (minor) | tut-01 |
| 1 | headless "expected result" render | `renderProject`'s own `_frame` fits the camera to whatever is in the project, at any scale, the way `OrbitController.frame` (the interactive viewport's own fit, floor `1e-4`) does | `render_project.dart`'s `_frame` floors its fitted bounding radius at `0.05` (5 cm). Case 1's own teapot, correctly imported at millimetre scale, is about 8 mm across — six times under that floor — so an unscaled render frames a handful of pixels in the middle of a 512×512 image instead of filling it. Confirmed by rendering `case1ImportedProject()` before adding a render-only 20× scale-up workaround (`tool/make_case1_fixtures.dart`'s own `_scaledForRender`, never applied to the exported fixtures). A person driving the real GUI does not hit this — only the headless render/renderSheet tools, which is exactly what the tutorial's own screenshots and case 6's agent-facing renders both depend on | function, tool-specific | tut-02 |

| 2 | operation card on a mesh edit (`ModelHistory.amend`) | an agent or a person adjusting the last edit through `ModelSession`/MCP the way the app's own operation card does | **Resolved 2026-09-15 (tut-03).** Used to have no path at all: `ModelSession` had no `amend` method, only the app's own direct call to `_history.amend(to)` (`apps/flutter3d_modeler/lib/src/screen/interactions.dart`), bypassing `ModelSession`/`CommandJournal` entirely — `amend` itself worked (`tutorial_scenarios_test.dart`'s own case-2 group already showed it re-running a mesh edit against the document before it, not on top of it), but nothing recorded the adjustment, so a session's own `journal()` file still named the *original* argument after an amend. Fixed by a new `ModelSession.amend(to)` that calls through to the same `ModelHistory.amend`, then records `to` to `_journal` itself through a new `CommandJournal.amend` — forgetting the original line (the whole `begin`/`…`/`end` bracket, when the step being adjusted was a `transaction`) and writing the adjusted command in its place, so a cold replay lands on the adjusted state directly. Offered over MCP as the `amend` tool. New coverage: the case-2 amend test now asserts the journal names the adjusted distance, plus a second, selection-independent test (`addPrimitive`) proving a cold replay of a journal ending on an amend reaches the adjusted state byte-for-byte | agent-facing, integrity, resolved | tut-03 |
| 2 | mesh editing (extrude/loop cut/bevel) | the three mesh-edit tools the plan names for this case | **Resolved 2026-09-15 (tut-04).** Used to have no command at all: `flutter3d_mesh` had real `bevelEdges`/`bevelVertices` functions (`bevel.dart`), but nothing in `packages/flutter3d_model_core` wrapped either one, and `apps/flutter3d_modeler/lib/src/ui/tools.dart`'s own comment named bevel among the operations this repository does not have. Fixed by a new `BevelEdges` `ModelCommand` that reads the selection's own level to pick between the two functions — a vertex selection walks `bevelVertices`' own per-vertex edge fan directly, since converting a vertex selection to edges the ordinary way keeps only an edge with *both* ends selected and would silently under-bevel a lone corner — registered in `command.dart`'s reader table, offered over MCP as the `bevelEdges` tool, and given a real rail button (`mesh.bevel`, key `B`). Case 2's own scenario and fixtures are untouched — extending the case to actually exercise a bevel step is a separate, tutorial-content change now that the command exists, not folded into this one. New coverage: `commands_test.dart`'s own `BevelEdges` tests (matches `bevelEdges`/`bevelVertices` called directly, undo byte-exact, correct dispatch by selection level, and the same refusal a partial selection gets from the functions themselves) and `tool_commands_test.dart`'s own `mesh.bevel` pair | function, resolved | tut-04 |
| 2 | mesh editing (extrude/loop cut), modal transform | a case's own `.jsonl`, replayed through `CommandJournal.replay` from a cold `ModelProject`, rebuilds the document — the same promise case 1's journal keeps | **Resolved 2026-09-15 (tut-05, alongside tut-15).** `Extrude`/`LoopCut`/`TransformElements` all act on "whatever is currently selected", and selecting mesh elements was `ModelSession.select` assigning `ModelHistory.selection` directly rather than running a `ModelCommand` — case 2's own journal, replayed cold, used to refuse at the first `extrude` with "no faces are selected to extrude". Fixed by a new `SelectElements` command (`flutter3d_model_core`'s own `selection_commands.dart`) that `select` now runs through `ModelHistory.run`, at both object and mesh-element level, registered in `modelCommandNames`/`modelCommandFromJson` like any other command — paired with `tut-15`'s own fix making `ModelHistory.run`/`amend`/transactions record to whichever journal is attached regardless of caller, a pick now lands on the recovery journal the same as any edit. Case 2's own `case2.jsonl`, replayed cold, now rebuilds the exact document a live session reaches; the same fix closes the identical gap in cases 3 (object-level `MoveBy`) and 4 (mesh-vertex level) without a row of their own, since both already named this one as their root cause. New coverage: `history_journal_test.dart`'s own `SelectElements` group; `tutorial_scenarios_test.dart`'s case-2/3/4 cold-replay tests now assert success | integrity, agent-facing, resolved | tut-05 |
| 2 | modifier stack (mirror/array) | adding a modifier and seeing its effect — the viewport, a headless render, or an export all show what the stack currently produces | **Resolved 2026-09-15 (tut-06).** Used to be invisible everywhere but a one-way `Apply`: nothing read `ModelObject.modifiers` except the properties panel's own list (`apps/flutter3d_modeler/lib/src/ui/properties/properties_panel.dart`) and `ApplyModifier`, which baked the stack into the base mesh and removed it — `SceneSync._dataOf` (the live viewport) read `object.geometry` directly, and neither `render_project.dart` nor `render_sheet.dart` mentioned `modifiers` at all. Case 2's own page worked around this only for its own picture, evaluating the stack by hand (`tool/make_case2_fixtures.dart`'s own `_withModifiersBaked`), the same three lines `ApplyModifier.apply` runs, because nothing else in this build could produce that picture. Fixed by routing both mesh-upload paths through `ModifierEvaluationCache` (`mesh-40`/`mat-18`, built earlier and never called from the app): `SceneSync.apply` evaluates an object's own enabled modifiers over its base mesh and tracks `ModelObject.modifiers` by identity so a toggled or edited stack re-uploads even when the base geometry's own identity never moves; `render_project.dart`'s `renderProject` does the same with a fresh cache per call, and `render_sheet.dart` inherits the fix for free by calling `renderProject` per quadrant. `ApplyModifier` itself needed no change — it still bakes and empties the stack, now baking what was already visible. Case 2's own fixture tool no longer hand-evaluates anything (`_withModifiersDisabled`, a real live toggle, replaces `_withModifiersBaked`); regenerating it produced byte-identical PNGs to what was already committed, confirming the live cache folds a stack the same way the hand-rolled version did. New coverage: `scene_sync_test.dart`'s own `tut-06` group and `flutter3d_cpu/test/render_project_test.dart`'s own `tut-06` group, covering a mirror, an array, a mid-session toggle, and `ApplyModifier` still baking to the same picture the live stack already drew | function, resolved | tut-06 |
| 3 | Scene mode (light/shadow/environment/post), headless "expected result" render | `renderProject`/`renderSheet` — the same functions this tutorial's own reference pictures come from — draw a project's real `AddLight`/`SetLightField`/`SetEnvironment`/`SetSceneLightingField` settings, the way case 3's own point light, shadow request, studio environment and bloom should show up in its "what you get" picture | **Resolved 2026-09-15 (tut-07).** Used to draw neither the live viewport nor a headless picture from a project's own `SceneLighting`: `render_project.dart`'s own `renderProject` built a `Scene` from two fixed, hardcoded directional lights and never read `ModelProject.lighting` at all, and `ModelerStage.fromProject` never called `LightingSync` either — confirmed directly, `grep -rn "LightingSync(" apps/flutter3d_modeler/lib/` found exactly one call site, a throwaway, never-drawn `lightOverflowOf`. Fixed by moving `LightingSync` out of the application into `flutter3d_model_core` (`lighting_sync.dart`) — nothing it touches (`Scene`/`LightNode`/`RenderSettings`) needs the Flutter SDK, and this package already depended on `flutter3d_core` for `render_project.dart` itself — so the identical class now wires the live viewport (`ModelerStage.fromProject`, `modeler_cubit.dart`'s own re-sync, and `ready_parts.dart`'s own `RenderSettings`), the game-preview route (which shares that same stage and settings), and both headless render tools, all additively over the fixed key/fill pair rather than replacing it — a project that has never added a light of its own still renders exactly as it always did, confirmed empirically: every existing frame-test golden (`skeleton-overlay`, `modeler-autorig`, `modeler-retarget`, `game-preview`, and `flutter3d_render_job`'s own byte-for-byte snapshots) still matches its committed reference image unchanged. `ambientIntensity` and `environment` stay unwired — their defaults do not match the engine's own, so folding them in would have rebrightened every existing picture for a field nothing asks to see — a real, separate, narrower gap. New coverage: `flutter3d_model_core/test/lighting_sync_test.dart` (moved from the app, plus an additive-sync case), `flutter3d_cpu/test/render_project_test.dart`'s own `tut-07` group (a point light measurably brightens a headless render; `exposure` measurably dims one), `flutter3d_render_job/test/scene_from_project_test.dart` (a project's own light joins the fixed pair). Case 3's own page and fixture (`03-lit-corner.png`) are regenerated: the point light and its warm bloom now really show | function, resolved | tut-07 |
| 3 | import a second asset into an open project, from the app's own UI | a person clicking through the real GUI can merge a second file into a project already open — `mat-24`'s own extended scope (2026-09-09 decision) named this "Import into scene" via `ImportInto` | **Resolved 2026-09-15 (tut-08).** Used to have no call site at all: nothing in `apps/flutter3d_modeler` called `ModelSession.import` or the free `importInto` function, and the toolbar's only file action, **Open**, always replaced the whole document. Fixed by giving `TopBarActions` a real **Import** button beside Open, wired to a new `_importFile` extension method (`screen/files.dart`) that reuses `_openFile`'s own picker and the import screen's own unit/axis/cleanup choices, then runs `importInto` against the document already open and lands the merged project through `ReplaceDocument` — the same command `ModelSession.import` itself already runs, for the same reason (not journalable). The cleanup checkboxes now take an `only` set of object ids, so a second file's own weld/normals/triangulate choice cannot reach back into an object that was already open. New coverage: `top_bar_actions_test.dart`'s own Import-button test, and `file_import_test.dart` — a widget test driving the real toolbar button through a faked `FileSelectorPlatform`, proving the merge and proving Open still fully replaces the document. Case 3's own tutorial page now says the button is real; its own fixture script stays command-layer-driven, since that was always for the journal reason above and never for the missing button | function, app-facing, resolved | tut-08 |
| 4 | bending a joint (weights sub-mode's bend slider) | an agent over MCP, or a headless case, can reach the same pose a person's live bend slider previews | `ui/bend_slider_bar.dart`'s own doc comment is explicit: a bend turns the joint's live `SceneNode` directly and "never touches `ModelHistory`" — there is no `ModelCommand` behind it at all, by design (sixty frames of a drag are meant to cost nothing on the undo stack). The document-level equivalent an agent or a headless case actually has is `select` + `RotateBy` on the joint's own persisted `ModelObject.transform`, then `PoseJoint` to key it — case 4's own scenario uses exactly this, reaching the identical document state a person's slider drag would once *keyed*, through a different door than the live preview | agent-facing, by design | tut-09 |
| 4 | headless "expected result" render of a rigged, posed, morphed character | `renderProject`/`renderSheet` draw a project's real skin deformation and shape-key blend, the way case 4's own bent elbow and puffed chest should show up in its "what you get" picture | **Resolved 2026-09-15 (tut-10).** Used to draw neither: `render_project.dart` never mentioned "skin" or "skeleton" anywhere in it — every mesh drew at its raw bind-pose position regardless of `ModelObject.skeletonIndex` — and it never mentioned `shapeSet` either. Fixed by folding both into `_meshDataFor`: a shape-key blend (`ShapeKey.blend`'s own delta formula, at the object's current `shapeSet.weights`) and a skin pose, both read off the document as it stands, no animation curve involved. The skin pose is a CPU bake (`_withSkin`), not the engine's own GPU `Skeleton`/`MeshNode.skeleton` pipeline `SceneSync` poses the live viewport with — checked directly against the real `RobotExpressive.glb` fixture, that pipeline's own local-space bind convention (glTF's) disagreed with `buildSkeleton`'s own world-space one, and the torso collapsed to a hundredth of its size and vanished the first time this actually got rendered. `_withSkin` composes an extra `meshWorld` term that reconciles the two conventions; this is a real, pre-existing gap in the auto-rig's own bind-matrix convention (for any mesh whose own object transform is not the identity), not introduced by this fix — only the first thing to render its result and so the first to notice it, worth its own row rather than folded into tut-10's own scope. New coverage: `flutter3d_cpu/test/render_project_test.dart`'s own `tut-10` group. Case 4's own fixture (`07-character-real-geometry.png`) is regenerated: the bent elbow and the puffed chest both really show now | function, resolved | tut-10 |
| 4 | headless render of the weight-paint gradient shading mode | `render`'s own `mode` argument (or some fourth one) draws the same weight-gradient visualization S4 built for the live viewport, so a case can show what a brush stroke actually painted without a window | **Resolved 2026-09-15 (tut-11).** Used to be unreachable at any layer: `RenderShading` (`render_project.dart`) had exactly two members, `material` and `normals`. Fixed by adding a third, `weights`, plus a `RenderRequest.weightsJoint` naming which joint to paint for — `weight_gradient_colors.dart` restates `weight_gradient.dart`'s own five-stop sRGB gradient locally (`flutter3d_model_core` still has no path to the application it lives in, so a dozen lines copied once is cheaper than a package built to share them), and `_withWeightGradient` bakes it into the vertex colour channel of whichever object is bound to the requested joint, using `weightsOf` — already a plain-Dart `flutter3d_mesh` dependency, no windowed-engine concept needed after all. `render`/`renderSheet` (`render_tool.dart`) gained `'weights'` mode and a `joint` argument. New coverage: `flutter3d_cpu/test/render_project_test.dart`'s own `tut-11` group, checking both of the gradient's own extreme stops actually appear in a real render. Case 4's own `02-weight-paint-gradient.png` fixture is now a real render (the left elbow's own weight) rather than a placeholder | function, resolved | tut-11 |

| 5 | retarget: lock feet correction | `retargetClip(..., lockFeet: true)` (the default) corrects each leg's own foot position against `groundY` after retargeting, the way screen 14's own "Lock feet" toggle promises | **Resolved 2026-09-15 (tut-12).** Used to throw instead, for a real fixture: `RiggedFigure.glb`'s own clip animates every joint's translation, rotation *and* scale, not only the root's, so `_lockFeet`'s own `tracksByNodeId` (`Map<int, RigTrack>`, one entry per target joint) kept only the *last* of a joint's three retargeted tracks — the scale one — and dropped the rotation it actually needs; its own per-key indexing math then read that three-floats-per-key buffer as four-floats-per-key quaternions and ran past its own end. Confirmed directly at the time: `tutorial_scenarios_test.dart`'s own case-5 group called `RetargetClipJobRequest(..., lockFeet: true).run()` against this exact pair of rigs and asserted the `RangeError`. Fixed by keying `_lockFeet`'s own lookup by node *and* path instead of node alone, so none of a joint's tracks are dropped; case 5's own scenario now retargets with `lockFeet` at its own default (`true`) rather than working around the crash, and its fixtures and page are updated to match | function (bug), resolved | tut-12 |
| 5 | retarget: auto-map | screen 14's own "Auto-map" button (`looseAutoMap`) reads a real, if unusually named, source rig's joints and matches at least the ones a person would recognise by eye | **Partially resolved 2026-09-15.** Used to map nothing at all for any name whose side marker sat mid-word: `looseAutoMap` read only two rig-family conventions (Mixamo's `mixamorig:LeftUpLeg`, 3ds Max Biped's `Bip01_L_Thigh`), both of which put the side marker at the very start of a bone's own name once its prefix is stripped, so `RiggedFigure.glb`'s own names (`leg_joint_R_1`, `arm_joint_L_2`, side in the *middle*) went entirely unread. Fixed: `_looseSide` now also reads a side marker as a delimited token anywhere in the name (split on `_`/`.`/`-`/space and camelCase boundaries), so a name like Blender's own `UpperArm_L`/`Thigh_R` auto-maps correctly today where it used to refuse outright — checked directly, and the two prior start-anchored conventions are unchanged. `RiggedFigure.glb` itself still returns an empty `BoneMap`, confirmed directly again after the fix — its own gap turns out to be narrower than "side marker position": its joint words (`torso`/`arm`/`leg`/`neck`) are generic placeholders in neither synonym table, and `arm_joint_L_1`/`_2`/`_3` differ from each other only by a trailing chain index (shoulder/elbow/wrist), a distinction no word lookup can make regardless of where the side marker sits. Case 5's own scenario keeps its seventeen-row hand correction, now guarded by a canary test (`loose_auto_map_test.dart`) so a future widening that actually closes this file's own case is caught rather than assumed. The remaining path — a Khronos-sample-specific synonym table keyed on this file's own words plus their chain index — is a separate, still-open piece of `tut-13`, not scheduled | function (coverage), partially resolved | tut-13 |
| 5 | retarget: apply, crash recovery | a case's own `.jsonl`, replayed through `CommandJournal.replay` from case 4's own saved project, rebuilds the document — the same promise case 1's journal keeps | **Resolved 2026-09-15 (tut-14).** Used to refuse outright: `ApplyClipResult` recorded to the journal fine (`ModelSession.run`'s own `_journal.record` writes any `ModelCommand`, registered or not) but was deliberately outside `modelCommandNames`/`modelCommandFromJson` — a cold replay refused at the very first line with "names a command this build does not know," a different shape than cases 2–4's own `tut-05` (a command this build *does* know, refused only for want of a selection nothing recorded): here the build could not even name the command, selection or not. Fixed by registering `ApplyClipResult` in `modelCommandNames`/`modelCommandFromJson` (`command.dart`), reading a clip back through new `rig_job_commands.dart` helpers that reuse `keyframe_commands.dart`'s own `_pathFrom`/`_interpolationFrom`/`_doubleListFrom`; `ApplyJobResult`, checked directly, was already registered. Offered over MCP as the `applyClipResult` tool, the same "land a result computed elsewhere" shape `applyJobResult`/`applySimulationCache` already use. New coverage: the case-5 cold-replay test now asserts success and checks the replayed project is byte-identical to `case5.f3dproj` | integrity, resolved | tut-14 |

| 6 | agent session: recovery journal over a shared, mixed-authorship document | a session's own recovery `.jsonl`, replayed cold, rebuilds the document the way it was left — the same promise case 1's journal keeps, now with a person editing the same shared `ModelHistory` an agent's `--mcp-port` session is bound over (`mcp_bootstrap_io.dart`'s own doc comment: "the same document a person already has open") | **Resolved 2026-09-15 (tut-15, alongside tut-05).** A person's own edit — run directly against `ModelHistory`, the exact door `ModelerCubit.run` uses, never through `ModelSession.run` — used to never reach `CommandJournal.record` at all: `tutorial_scenarios_test.dart`'s own case-6 group replayed `case6.jsonl` cold and got `replay.ok == true` with no refusal, but the replayed material's own roughness read `SetMaterialField`'s default (`0.5`), not the real project's `0.35` a person actually set. Fixed by giving `ModelHistory` itself an optional `recoveryJournal` that `run`/`amend`/`beginTransaction`/`endTransaction` record to on every success, whichever caller reached them, under whatever author it was actually given — `ModelSession`'s own constructor attaches a fresh journal to whichever `ModelHistory` it is handed, so `--mcp-port`'s shared history journals a person's own edit exactly where it happened. `ReplaceDocument` stays off the journal on purpose, now via a new `ModelCommand.isJournaled` flag on the command itself rather than an accident of which caller happened to bypass `ModelSession.run`. Case 6's own `case6.jsonl`, replayed cold after a mixed agent/person session, now reproduces the 0.35 roughness under `StepAuthor.person`. New coverage: `history_journal_test.dart`'s own attached-journal group; `tutorial_scenarios_test.dart`'s case-6 cold-replay test now asserts the real roughness and author instead of the silent default | integrity, agent-facing, resolved | tut-15 |
| 6 | screen 26, "Agent session" (tool-call feed, "You"/"Agent" history badges, six-view contact sheet) | a live panel showing an agent's own tool calls and history alongside a person's, so both parties can see whose step is whose — the same distinction `mcp-10n`'s own undo restriction already enforces underneath | **Resolved 2026-09-15 (tut-16).** Used to not exist at all — confirmed directly by reading `apps/flutter3d_modeler/lib/src/mcp_ui_tools.dart`, which offered exactly the seven `ui.*` tools `mcp-16d` scoped and nothing else. Fixed by an `onCall` hook on `flutter3d_mcp_kit`'s own `ToolTableServer`, run after every tool call answers and threaded down through `ModelHttpServer.start` to a new `ModelerCubit.agentToolCalled` — the one seam that both feeds a bounded tool-call list and resyncs the scene when an agent's own edit lands straight on the shared `ModelHistory`. `ModelerShell` grew an `agentPanel` slot, appended after the ordinary properties panel, holding the new `AgentSessionPanel`: the feed, and a history list reading `HistoryStep.author` as "You"/"Agent" badges (newest first) with an "Undo agent steps" button gated on `ModelHistory.topStepAuthor` — `mcp-10n`'s own restriction, finally visible rather than only reachable through the status line's own undo sentence. The contact sheet (`AgentContactSheet`, in `ModelerShell.bottom`) reads the pictures a `render`/`renderSheet` call actually drew this session, most recent first, rather than a second live six-camera renderer nothing else in this app stands up — see `agent_session_panel.dart`'s own library comment for why that is the truer "what the agent gets instead of numbers." New coverage: `mcp_bootstrap_test.dart` (the hook fires over a real socket), `modeler_cubit_test.dart` (the feed, its bound, and the restricted undo), `agent_session_panel_test.dart` (a fake tool call landing live, and the two badges reading distinctly) | UX, app-facing, resolved | tut-16 |

The rows below were not surfaced by any one case; they are the plan's own
five candidates named before a case was written (`doc/model-editor-plan.md`
§ tut-00, "Known candidates"), checked directly against this branch during
T6's close-out pass, plus two more from re-reading all six pages together.
Two of the five candidates are no longer true and get no row: a `.gltf`
beside its `.bin` opens on the web and on macOS today (`ui-36n`, recorded
`done`), and the status line already shows "vertices/materials" (`mat-33d`).
The other three are below, alongside two fresh findings from re-reading
`ui-37d`'s own text, which named both as latent quirks moved verbatim from
`main.dart` and promised each "its own row" — a promise this pass keeps.

| case | step | expected | actual | type | plan row |
|---|---|---|---|---|---|
| — | drag a file onto the window (`_handleDroppedFile`) | dropping a supported file installs it as the open document, the same as the toolbar's own **Open**, once the import screen's unit/axis questions are answered | **Resolved 2026-09-15 (tut-17).** Used to not: `_handleDroppedFile` (`apps/flutter3d_modeler/lib/src/screen/files.dart`) awaited `_openBytesWithImportScreen` and discarded the returned `FileOpened` outright — confirmed by reading the function directly, next to `_openFile`'s own call to the same method a few lines above, which switches on the result and calls `_installOpened`. A person drags a file, answers the import screen's questions, and nothing on screen changed. This was the plan's own known candidate ("a dropped file is decoded and then thrown away") and the first of the two latent quirks `ui-37d`'s own text named when the handler moved into `screen/files.dart` verbatim. Fixed by having `_handleDroppedFile` call `_openBytes` — the same shared helper `_openFile` already calls, switch and `_installOpened` included — instead of `_openBytesWithImportScreen` directly; `file_drop_io_test.dart`'s own `tut-17` group pumps the real `ModelerScreen`, drops a project file through the real `desktop_drop` channel, and checks the top bar's own document-name label changes to match | function (bug), resolved | tut-17 |
| — | open a second document (`TransformSession`'s other-object picker cache) | opening a new document forgets every mesh-picking cache keyed by the previous document's object ids, the way `_installOpened` already forgets `_elementPickerCache` for exactly this reason ("ids start again in the new project ... could match a version and answer about a mesh that is gone") | **Resolved 2026-09-15 (tut-18).** Used to not be cleared: `TransformSession._otherPickers` (`Map<int, ({int version, MeshPicker picker})>`) was never cleared by `_installOpened` or anywhere else — confirmed directly, no call site outside its own lazy-build path touched it. A new document whose object ids collide with a cached id and version could read a stale picker built against a mesh that is gone. The second of `ui-37d`'s own two named latent quirks, found fresh while reading `TransformSession` alongside the row above. Fixed by giving `TransformSession` its own `forget()`, called from `_installOpened` right beside `_elementPickerCache.forget()`; `transform_session_test.dart`'s own `tut-18` group builds a same-id/same-version collision across two documents and checks the stale picker is gone after `forget()` — and, without it, that the same collision really does still answer with the first document's own stale corner | integrity, resolved | tut-18 |
| — | cabinet: a model's detail page | a model uploaded to the cabinet shows a preview picture | still true, and already tracked, not a fresh finding: `cloud/README.md`'s own "What is not here yet" says outright the schema and repository have a place for one but nothing makes one yet, pending a browser-side capture the viewer cannot read back. The plan's own "out of scope" list already names this "a separate `cloud` row" | UX | tut-19 |
| — | cabinet: re-open a model | a model opened from the cabinet's own viewer can be edited and saved back to the same entry, as a revision | still true, and already tracked, not a fresh finding: the same "What is not here yet" section says editing from the cabinet is real but saving back is its own next stage, with revisions | UX | tut-20 |

Case 1 ("A prop from a scan"), case 2 ("A vase from a profile"), case 3
("A lit corner"), case 4 ("A character from a bare mesh"), case 5
("Borrowing a walk: retarget a clip") and case 6 ("An agent beside you")
are the six cases the plan names; all six are now written and pass as
`.jsonl` scenarios (`tutorial_scenarios_test.dart`). None has actually been
walked by hand on a real macOS machine yet — every page's own "Time to
complete" line is still a `TODO`, and every screenshot of the running app's
own chrome (panels, dialogs, buttons) is still an explicit "screenshot
pending" placeholder; only the pure-3D "expected result" pictures (headless
`render`/`renderSheet`, deterministic, checked in CI) are real on all six
pages. This is the acceptance rule's own second condition, not yet met.
Every gap row above — sixteen found writing the six cases, four more found
closing the pass out — has become a `tut-NN` line in
`doc/model-editor-plan.md` with a decision, per the rule's first condition.
