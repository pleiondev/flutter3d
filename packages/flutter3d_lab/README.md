# flutter3d_lab

Virtual laboratory simulations built on `flutter3d_sim`'s stepping and
recording primitives — `edu-04`'s pendulum is the first.

Plain Dart, no Flutter SDK: a server verifies a student's submitted lab run
the same way it verifies a game run — by replaying it, in a container with
no Flutter SDK in it.
