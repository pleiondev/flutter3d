## 0.1.0

* Collision shapes, a broadphase grid, ray and overlap queries, rigid bodies and
  a character controller that walks slopes, steps and ramps.
* No Flutter and no renderer in it, which is the boundary the package exists to
  keep: it runs under `dart test`.
