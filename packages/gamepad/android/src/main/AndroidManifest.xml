<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- No permissions. A gamepad needs none: `InputManager` and the motion
         events a focused window already receives are all this plugin reads. -->
</manifest>
