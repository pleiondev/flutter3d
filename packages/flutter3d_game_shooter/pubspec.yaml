name: flutter3d_game_shooter
description: "The parts of a first-person shooter that are not parts of an engine: monsters, weapons, an inventory, and the step order that ties them together."
version: 0.5.0
repository: https://github.com/pleiondev/flutter3d
homepage: https://flutter3d.pleion.dev
issue_tracker: https://github.com/pleiondev/flutter3d/issues
topics:
  - game-development
  - game-engine
  - shooter
  - gameplay
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # The engine half: the fixed step, input, levels, actors, mechanisms, the ECS.
  # Everything here is content those take as arguments.
  flutter3d_game: ^0.5.0

  # Named rather than leant on through the game layer's re-export: this package
  # builds colliders and worlds itself, and a dependency you use is a dependency
  # you declare. The same reason names the simulation, which is where the fixed
  # step, the levels, the actors and the saves live since they stopped needing
  # Flutter — `flutter3d_game` still re-exports it, and this package uses it
  # directly enough to say so.
  flutter3d_physics: ^0.5.0
  flutter3d_sim: ^0.5.0

  # Only `bridge.dart` imports this — the view model of a weapon held in the
  # hands is a shooter's, not a renderer's and not a bridge's. The simulation
  # library imports nothing from it, and a test holds that.
  flutter3d: ^0.5.0

  vector_math: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# Why this package exists: `flutter3d_game` used to ship a monster roster, a
# weapon roster and an entity-type list, and `flutter3d_bridge` referenced them,
# so a second game inherited this one's vocabulary. The engine packages know
# what a body, a brain and a mechanism are; what a monster is, what a shotgun
# does and what a step of a shooter looks like are here.
