## 0.1.0

* The screens a game has that are not the game: a settings panel with volumes,
  rebinding and accessibility, the gear that opens it, credits, and where a save
  and a settings document are kept on each platform.
* `DragLook` and `clockText`, which every game had written for itself.
