## 0.1.0

* What a game is as an application: the surface a rendered frame reaches Flutter
  through, and the run being played — loading, resuming, moving on, failing.
* `FrameClock`, which answers how long since the last frame and says nought on
  the first one.
