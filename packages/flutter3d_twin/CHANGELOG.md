## 0.1.0

- **The spindle reading, `tpl-04`'s own twin.** A digital-twin data source
  built on `flutter3d_sim`'s reading and binding primitives, moved out of
  `flutter3d_template_app` so a server can check or replay it with no
  Flutter SDK in the container.
