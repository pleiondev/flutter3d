## 0.1.0

* The repository's package rules as tests a package runs against itself: it names
  no genre, it depends on no application, and it shares no mutable value as a
  constant.
* Pure Dart, so the one package with no Flutter in it can use it too.
