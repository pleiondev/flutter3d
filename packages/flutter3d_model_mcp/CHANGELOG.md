## 0.6.0

**An entry point that says what it cannot do yet, and a graph that proves what
it can.** `dart run flutter3d_model_mcp:model_mcp --help` resolves and prints
usage on a machine with the Dart SDK and no Flutter — which is the property the
whole package split below it exists to keep, checked from the first commit
rather than after the server is written.
