name: flutter3d_webgl
description: "A WebGL2 backend for flutter3d: the second implementation of flutter3d_graphics, and the one that tells us whether the HAL is a seam or a description of Impeller."
version: 0.1.0
repository: https://github.com/dzolotov/flutter3d
publish_to: 'none'
resolution: workspace

# Depends on the HAL and on nothing else of ours — in particular **not** on
# `flutter3d`, and not on `flutter3d_impeller`. That is the property the split was
# for: writing a backend means implementing `flutter3d_graphics`, not understanding
# the renderer.
environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  flutter3d_graphics: ^0.1.0

  # Browser APIs through static JS interop. `dart:html` and `dart:web_gl` are
  # the legacy path and are not available under Wasm.
  web: ^1.1.0

  vector_math: ^2.2.0

dev_dependencies:
  # The rules this package keeps, as tests it runs against itself. See
  # flutter3d_boundaries.
  flutter3d_boundaries: ^0.1.0
  # The behaviour flutter3d_graphics requires, as tests this backend runs
  # against itself. See flutter3d_conformance.
  flutter3d_conformance: ^0.1.0
  # For the tests only: engine_frame_test drives the real Renderer through this
  # backend, which is the one thing this package exists to make possible and so
  # the one thing worth testing. A dev dependency, so nothing a consumer builds
  # picks up the engine because the backend was installed.
  flutter3d: ^0.1.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
