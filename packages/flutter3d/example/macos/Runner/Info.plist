<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Flutter GPU is opt-in per app, not just per Flutter channel. Without this
	     key ShaderLibrary.fromAsset throws at startup. The equivalent CLI form is
	     `flutter run --enable-flutter-gpu`; on Android the key is
	     io.flutter.embedding.android.EnableFlutterGPU in AndroidManifest.xml. -->
	<key>FLTEnableFlutterGPU</key>
	<true/>
	<!-- Impeller is not the default renderer on macOS yet, and Flutter GPU
	     requires it. Without this key the app only works when launched as
	     `flutter run -d macos --enable-impeller`; opening the built .app
	     directly fails with "Flutter GPU requires the Impeller rendering
	     backend". -->
	<key>FLTEnableImpeller</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string></string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSHumanReadableCopyright</key>
	<string>$(PRODUCT_COPYRIGHT)</string>
	<key>NSMainNibFile</key>
	<string>MainMenu</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
</dict>
</plist>
