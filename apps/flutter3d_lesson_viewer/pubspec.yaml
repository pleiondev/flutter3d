name: flutter3d_lesson_viewer
description: "A read-only player for an edu-00 lesson document: no walking body, no authoring — a camera stepped through an edu_sequence."
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # The backend chooser with its software fallback — see `lib/src/backend.dart`.
  flutter3d_app:
    path: ../../packages/flutter3d_app

  flutter3d:
    path: ../../packages/flutter3d

  # The document and the level format.
  flutter3d_game:
    path: ../../packages/flutter3d_game
  flutter3d_sim:
    path: ../../packages/flutter3d_sim

  # A level document to mesh nodes and lights, and `WidgetSurfaceVisuals` for
  # the `widget_surface` entities a lesson's annotations render through.
  flutter3d_bridge:
    path: ../../packages/flutter3d_bridge

  # `orderedSteps` — resolving an `edu_sequence.steps` name list to the
  # `edu_step` entities themselves. Headless, plain Dart, already off the
  # Flutter SDK for the same reason `flutter3d_editor` needs it there.
  flutter3d_editor_core:
    path: ../../packages/flutter3d_editor_core

  # The surface a frame reaches Flutter through.
  flutter3d_session:
    path: ../../packages/flutter3d_session

  vector_math: ^2.2.0

  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # A renderer with no GPU under it, so a test can load a level and pump a
  # widget the way the app does, without a window.
  flutter3d_cpu:
    path: ../../packages/flutter3d_cpu

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/levels/
